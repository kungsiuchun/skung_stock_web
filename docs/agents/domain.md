# Domain docs

This is a single-context repository.

Before exploring a feature area, read `CONTEXT.md` and only the relevant files in `docs/adr/`. Use the glossary's defined terms in issues, specs, tickets, and tests. Surface an ADR conflict rather than silently overriding it.
